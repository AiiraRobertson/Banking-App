[build]
  base    = "client"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback so React Router routes (e.g. /dashboard, /transfer) resolve to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
