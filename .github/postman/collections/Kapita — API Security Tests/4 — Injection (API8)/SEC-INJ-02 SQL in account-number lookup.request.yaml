$kind: http-request
url: "{{baseUrl}}/api/accounts/lookup?account_number=' OR '1'='1"
method: GET
headers:
  Authorization: Bearer {{userToken}}
queryParams:
  account_number: "' OR '1'='1"
scripts:
  - type: afterResponse
    code: |-
      pm.test('no row leaked', () => {
        let body = {}; try { body = pm.response.json(); } catch(e) {}
        pm.expect(body.found).to.not.equal(true);
      });
    language: text/javascript
order: 2000
